from __future__ import annotations

from typing import Any, Dict


def run(config: Dict[str, Any], runtime_ctx: Any = None, **kwargs) -> None:
    print("[gd.eval.teacher_vis] TODO: port legacy eval to direct run(config) interface.")

